// Frontend-side constants. Kept here (not imported from src/store) so the web bundle never pulls in
// bun:sqlite. Must match src/store/config.ts's DEFAULT_OPEND_PORT.
export const DEFAULT_OPEND_PORT = 33334;
